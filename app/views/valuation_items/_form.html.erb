<%= form_for(@valuation_item) do |f| %>
  <% if @valuation_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@valuation_item.errors.count, "error") %> prohibited this valuation_item from being saved:</h2>

      <ul>
      <% @valuation_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
